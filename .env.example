GITHUB_TOKEN=your_github_token_here
PORT=8001
# Base64 encoded RSA private key (generate with: openssl genrsa -out app.rsa 2048 && base64 -w 0 app.rsa)
APP_RSA=base64_encoded_rsa_private_key
# Base64 encoded RSA public key (generate with: openssl rsa -in app.rsa -pubout | base64 -w 0)
APP_RSA_PUB=base64_encoded_rsa_public_key
